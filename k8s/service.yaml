apiVersion: v1
kind: Service
metadata:
  name: ess-mcp-server
  namespace: ess-mcp
  labels:
    app: ess_mcp_server
spec:
  type: ClusterIP
  selector:
    app: ess_mcp_server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
