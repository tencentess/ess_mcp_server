services:
  ess-mcp-server:
    image: ccr.ccs.tencentyun.com/pulse-line-prod/ess-mcp-server:latest
    container_name: ess-mcp-server
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./log:/app/log
